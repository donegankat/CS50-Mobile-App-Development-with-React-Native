import {StyleSheet} from 'react-native'

export const themeStyles = StyleSheet.create({
    darkContainer: {
        backgroundColor: '#242C40',
    },
    darkThemeText: {
        color: '#D0D0C0',
    },
});